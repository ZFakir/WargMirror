# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.js >> login page/script logic >> should handle validation errors
- Location: tests\login.spec.js:12:3

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected substring: "Incorrect email or password"
Received string:    "Login failed. Please check your credentials."
```

```
Error: coverage.stopJSCoverage: Test ended.
```

# Test source

```ts
  1  | const { test: base, expect } = require('@playwright/test');
  2  | const fs = require('fs');
  3  | const crypto = require('crypto');
  4  | const path = require('path');
  5  | 
  6  | const test = base.extend({
  7  |   page: async ({ page }, use) => {
  8  |     // Start coverage
  9  |     await page.coverage.startJSCoverage({ resetOnNavigation: false });
  10 |     
  11 |     await use(page);
  12 |     
  13 |     // Stop coverage
> 14 |     const coverage = await page.coverage.stopJSCoverage();
     |                                          ^ Error: coverage.stopJSCoverage: Test ended.
  15 |     
  16 |     const v8dir = path.join(process.cwd(), '.v8-coverage');
  17 |     if (!fs.existsSync(v8dir)) {
  18 |       fs.mkdirSync(v8dir, { recursive: true });
  19 |     }
  20 |     
  21 |     fs.writeFileSync(
  22 |       path.join(v8dir, `coverage-${crypto.randomUUID()}.json`),
  23 |       JSON.stringify({ result: coverage })
  24 |     );
  25 |   }
  26 | });
  27 | 
  28 | module.exports = { test, expect };
  29 | 
```